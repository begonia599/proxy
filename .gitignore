# Secrets — never commit
.env
../.env

# Build artifacts
*.exe
claude-proxy
claude-proxy~
claude-proxy.bin
claude-proxy.bak-*
claude-proxy.new

# SQLite database (contains generated proxy keys + request logs)
*.db
*.db-wal
*.db-shm

# Legacy log format
usage.jsonl

# IDE
.vscode/
.idea/
*.swp

# Editor backups / scratch
*~
server.log

# Claude Code session artifacts
.claude/
